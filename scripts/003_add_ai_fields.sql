-- AI categorization feature has been removed
-- This migration file is kept for reference but no longer adds AI fields

-- Create index for regular sentiment analysis if needed in future
-- CREATE INDEX IF NOT EXISTS idx_feedback_sentiment ON feedback(sentiment);
